let num: number = 10, num2 = 20;

if (num < num2) {
    console.log(`${num} is less then ${num2}`)
}

// Ternary operator
let x: number = 10, y = 20;
x > y ? console.log('x is greater than y.') : console.log('x is less than or equal to y.')